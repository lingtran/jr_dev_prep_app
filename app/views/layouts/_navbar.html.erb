<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="nav_container">
    <% if current_user %>
      <%= link_to "Home", profile_path, class: "btn btn-default navbar-btn navbar"%>
      <%= link_to "Logout", logout_path, method: :delete, class: "btn btn-default navbar-btn" %>
    <% else %>
    <%= link_to "Home", root_path, class: "btn btn-default navbar-btn navbar"%>
      <%= link_to "Login", login_path, class: "btn btn-default navbar-btn navbar"%>
    <% end %>
  </div>
</nav>
